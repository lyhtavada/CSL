Run the /dfy-tracker skill for BOTH Joy and Chatty for LAST month (the calendar month before the current one).

Today is the 2nd of the month, so the target period is the entire previous month: from the 1st to the last day of last month. Compute the exact startDate (first day of last month) and endDate (last day of last month) yourself from today's date.

Follow skills/dfy-tracker/SKILL.md exactly. Do BOTH apps:

Shared:
- Read AVD_TICKET_API_KEY from CSL/.env
- Fetch tags from https://avada-ts-a9cb0.web.app/api/external/tags
- Tickets live at data.tickets in the API response
- Identify DFY tickets (subject starts with [DFY]), open only (ticketStatus !== "closed"), exclude tsStatus="sale_request"
- Determine CS creator (members[].isCreate), MAP each CS to their KPI nickname using the mapping table in the skill. Group and label by nickname, NOT the raw displayName. Exclude liz_avada tickets with no tags.
- Ticket link: shortUrl is relative, prepend https://avada-ts-a9cb0.web.app

=== JOY (appName="JOY Loyalty") — TAG-BASED scoring (§6a) ===
- Point from scoring tags: DFY-1=75 (Required), coupon/banner/icon cộng dồn trần 25 (Recommended), DFY-video=50 (Video). Max 150.
- Table: Date | Ticket | Store | Status | Tasks | Tags | Point, Total row per CS.
- Save to reports/dfy/joy/joy-dfy-{YYYY-MM}.md

=== CHATTY (appName="Chatty") — TASK-BASED scoring (§6b) ===
- Point from tasks[], split into 3 blocks by task.title prefix: "AI Agent:" (50), "Chatbox:" (30), "Bonus"/video (50).
- AI & Chatbox = weight × (task ✓ in block / total task in block), rounded. Video = all-or-nothing (50 if the video task completed, else 0). Max 130.
- Tags are tracking-only, NOT scored.
- Table: Date | Ticket | Store | AI (50) | Chatbox (30) | Video (50) | Tags | Point, Total row per CS.
- Save to reports/dfy/chatty/chatty-dfy-{YYYY-MM}.md

After writing BOTH files, commit them to git with message "dfy-tracker: auto monthly report {YYYY-MM} (Joy + Chatty)" and print, for each app, the saved path plus the summary line (total tickets, CS count, total points).
