Run the mine-chat-faqs skill for BOTH apps, fully autonomously — do not ask me any questions, use the defaults below.

For each app:
  - Joy:    segment app_joy
  - Chatty: segment app_chatty,app_faqs
  - Window: use the EXACT --start/--end dates given below (last full Mon→Sun calendar week). Pass them straight to fetch_chats.py.
  - Fetch with scripts/fetch_chats.py (run from /Users/avada/CSL/skills/mine-chat-faqs), read the JSON + the app's KB (Joy: /Users/avada/claw-weebhook-crisp-chat/agents/joy-loyalty-agent/knowledge/, Chatty: /Users/avada/claw-weebhook-crisp-chat/agents/chatty-agent/knowledge/), cluster questions into feature categories, and write one correct standard answer per FAQ in English.
  - Dedup against prior dated files in /Users/avada/CSL/reports/weekly-faqs/{app}/ and mark each FAQ 'new' or 'recurring', with a "What changed since last run" block.
  - Write the output to /Users/avada/CSL/reports/weekly-faqs/{app}/{app}_{start}_{end}.md using the real window dates.

Do NOT copy anything into the agent KB or run /deploy-agent. Output stays under CSL/reports/weekly-faqs/ only. When done, print a one-line summary per app (file path + new/recurring counts).
