Run the /cs-weekly skill for BOTH apps (Chatty and Joy) for last week.

Today is Monday. The target period is Monday → Sunday of LAST week: i.e. the 7 days ending yesterday (Sunday). Compute the exact startDate (last week's Monday) and endDate (yesterday, Sunday) yourself from today's date.

Follow skills/cs-weekly/SKILL.md exactly. For EACH app (chatty, then joy):
- Pull metrics: python3 skills/cs-weekly/scripts/fetch_metrics.py --app {app} --start {start} --end {end} --compare --json  (returns this_week + prev_week for ▲▼; tickets created, DFY created, chats)
- Pull reviews: python3 skills/cs-weekly/scripts/fetch_reviews.py --slug {joyio|chatty} --start {start} --end {end} --compare --json  (this_week + prev_week; App Store, sort_by=newest)
- If any review is ≤3★ (check low_reviews in the output), scan Slack group G019ZF7GM7H (token SLACK_BOT_TOKEN_AVADA) for that review by app name + published date, attach the thread permalink to the "Review cần lưu ý" line in §2; if no match, note "(không thấy trong feed bad-review)" — do not fabricate a link. 4★+ does not trigger this.
- Cluster top issues from the period's TICKETS (python3 skills/cs-weekly/scripts/fetch_tickets.py --app {app} --start {start} --end {end} --json; [dfy] excluded by default; read subject+description, cluster into 3-5 themes by volume, each with a 1-line fix/KB pointer; the [bug] prefix marks bugs — flag any repeated bug). NOT from chats — chat is only the §2 count metric.
- Scan Slack channel C07RNAY9ZC6 for releases in the period using SLACK_BOT_TOKEN_AVADA; link permalinks via chat.getPermalink; keep only releases relevant to that app
- Pull Bot performance metrics: python3 skills/cs-weekly/scripts/fetch_bot_qa.py {chatty|joy} {start} {end} --compare > /tmp/{app}-botqa-{YYYY-W##}.json  (--compare tự pull tuần trước cho ▲▼). Returns: handle {resolveRatePct = (total−human_active)/total = % session bot tự xử human không vào; aiReplyCoveragePct; humanTakeoverPct; escalationRatePct; sessions; inbound; botReplies}, qa {verifyCoveragePct; correctionRatePct; verifyCorrectPct; verifiedInWeek; correctionsInWeek; topVerifiers; topCorrectors — top3 theo created_at trong tuần}, prevWeek {same shape}. Live từ cs2.avada.net /api/obs/metrics + /api/reviews + /api/corrections. Fill the report's "🤖 Bot performance" section (right after TL;DR): bảng Handle + bảng QA, mỗi metric có cột tuần trước (▲▼) lấy từ prevWeek. If qa.verifyCoveragePct < 30, add the ⚠️ "verify coverage thấp" nudge line. Empty top-list → "(chưa có lượt nào tuần này)".
- Fill the §2 metrics table; the "tuần trước" column comes from the prev_week block of the --compare output (live from source, no file needed); show ▲▼ % for tickets/chats and ▲▼ count for reviews
- Write the report using reports/weekly-cs/TEMPLATE.md. Fill §1-4 and the §6 win line from data. The §1 TL;DR must reflect the TICKET-based top issues + the §2 numbers (the hottest bug/theme this week), NOT "merchant vào hỏi…" chat phrasing — chat is only a count metric now. Leave §5 Coaching and the Shoutout/Focus lines as "_(Liz điền)_" placeholders (Liz reviews these). Keep it ~1 screen; hide empty sub-blocks. Write it to a temp file /tmp/{app}-cs-weekly-{YYYY-W##}.md (ISO week of the Sunday end date). Do NOT save into the repo or commit to git — reports live in Notion only.
- Push to Notion as a new sub-page under the app's parent page:
  python3 skills/cs-weekly/scripts/push_notion.py --parent {PARENT} --title "{App} CS Weekly — W## ({DD–DD/MM/YYYY})" --md /tmp/{app}-cs-weekly-{YYYY-W##}.md
  Parent page IDs: Chatty = 37bb0da449f180729d79fcfc6d43c35a, Joy = 37bb0da449f18054b553c00929e711cb. The title MUST include the date range.

- Notify the app's CS Slack channel via the Avada bot (ONLY after the Notion push succeeds — if push failed, skip the Slack post for that app, don't ping a broken link):
  python3 skills/cs-weekly/scripts/notify_slack.py --channel {CHANNEL} --title "{App} CS Weekly — W## ({DD–DD/MM/YYYY})" --tldr "{the TL;DR text}" --botqa-file /tmp/{app}-botqa-{YYYY-W##}.json --notion-url {URL printed by push_notion.py}
  CS channel IDs: Chatty = C07LZNWEUUD (chatty-cs), Joy = C07MSUX0VPA (joy-faqs). Token = SLACK_BOT_TOKEN_AVADA. The message posts AS LIZ (--as-user default ON → her name + avatar), Block Kit (header + TL;DR + 🤖 Bot performance block + "Xem full trên Notion" button). --botqa-file adds the Bot performance block (Handle: resolve rate + AI coverage + human takeover + escalation + volume with ▲▼; QA: verify coverage / correction rate / verify đúng + top verify + top correction, ⚠️ if coverage < 30%). The title matches the Notion sub-page title.

After pushing BOTH apps to Notion AND notifying both CS channels, print the Notion page URLs, the Slack post confirmations, plus the headline numbers (tickets / chats / DFY / reviews) for each app. Do NOT commit anything to git.
